version: 2

# Declare your raw table(s) here, e.g.:
#
# sources:
#   - name: raw
#     schema: raw
#     tables:
#       - name: weather_daily
#         description: One row per city per date as returned by Open-Meteo.
sources: []
